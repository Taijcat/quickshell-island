# quickshell-island
Dynamic floating island for quickshell for Hyprland. Ships with gruvbox, but is compatible with any theme thanks to a generic palette.

The following command needs to be run to access the Control Center Home. ``` qs ipc call controlCenter toggle ```
